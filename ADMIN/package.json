{
  "scripts": {
    "start": "cd BACKEND && npm install && node app.js",
    "build": "cd FRONTEND && npm install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "nodemon": "^3.1.10",
    "vite": "^7.1.12",
    "dotenv": "^16.4.5"
  }
}